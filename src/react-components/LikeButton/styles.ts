import styled from 'styled-components';

export const LikeBottonStyled = styled.button`
   display: flex;
   align-items: center;
   padding: .25rem 1rem;
   margin-top: .5rem;
   border: none;
   outline: 0;
   background-color: transparent;
   cursor: pointer;
   -webkit-tap-highlight-color: transparent;

   & svg {
      margin-right: .5rem;
   }
`